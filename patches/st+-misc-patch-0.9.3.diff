diff --git a/config.def.h b/config.def.h
index 2cd740a..315f8c9 100644
--- a/config.def.h
+++ b/config.def.h
@@ -5,7 +5,7 @@
  *
  * font: see http://freedesktop.org/software/fontconfig/fontconfig-user.html
  */
-static char *font = "Liberation Mono:pixelsize=12:antialias=true:autohint=true";
+static char *font = "Liberation Mono:pixelsize=0:antialias=true:autohint=false";
 static int borderpx = 2;

 /*
@@ -153,7 +153,7 @@ static unsigned int rows = 24;
 /*
  * Default colour and shape of the mouse cursor
  */
-static unsigned int mouseshape = XC_xterm;
+static unsigned int mouseshape = XC_left_ptr;
 static unsigned int mousefg = 7;
 static unsigned int mousebg = 0;
 
@@ -176,10 +176,11 @@ static uint forcemousemod = ShiftMask;
  */
 static MouseShortcut mshortcuts[] = {
 	/* mask                 button   function        argument       release */
-	{ XK_ANY_MOD,           Button2, selpaste,       {.i = 0},      1 },
-	{ ShiftMask,            Button4, ttysend,        {.s = "\033[5;2~"} },
+	{ XK_ANY_MOD,           Button2, clippaste,      {.i = 0},      1 },
+	{ XK_ANY_MOD,           Button3, clippaste,      {.i = 0},      1 },
+	{ ShiftMask,            Button4, kscrollup,      {.i = -1} },
 	{ XK_ANY_MOD,           Button4, ttysend,        {.s = "\031"} },
-	{ ShiftMask,            Button5, ttysend,        {.s = "\033[6;2~"} },
+	{ ShiftMask,            Button5, kscrolldown,    {.i = -1} },
 	{ XK_ANY_MOD,           Button5, ttysend,        {.s = "\005"} },
 };
 
@@ -346,7 +347,7 @@ static Key key[] = {
 	{ XK_Delete,        ControlMask,    "\033[3;5~",    +1,    0},
 	{ XK_Delete,        ShiftMask,      "\033[2K",      -1,    0},
 	{ XK_Delete,        ShiftMask,      "\033[3;2~",    +1,    0},
-	{ XK_Delete,        XK_ANY_MOD,     "\033[P",       -1,    0},
+	{ XK_Delete,        XK_ANY_MOD,     "\033[3~",      -1,    0},
 	{ XK_Delete,        XK_ANY_MOD,     "\033[3~",      +1,    0},
 	{ XK_BackSpace,     XK_NO_MOD,      "\177",          0,    0},
 	{ XK_BackSpace,     Mod1Mask,       "\033\177",      0,    0},
diff --git a/x.c b/x.c
index a0dc48c..a731789 100644
--- a/x.c
+++ b/x.c
@@ -2213,7 +2213,7 @@ run:
 		opt_cmd = argv;
 
 	if (!opt_title)
-		opt_title = (opt_line || !opt_cmd) ? "st" : opt_cmd[0];
+		opt_title = (opt_line || !opt_cmd) ? "st+" : opt_cmd[0];
 
 	load_config(); //Loads values from config file
 	setlocale(LC_CTYPE, "");
diff --git a/st.1 b/st.1
index 39120b4..c88f736 100644
--- a/st.1
+++ b/st.1
@@ -1,8 +1,8 @@
-.TH ST 1 st\-VERSION
+.TH ST+ 1 st+\-VERSION
 .SH NAME
-st \- simple terminal
+st+ \- simple terminal+
 .SH SYNOPSIS
-.B st
+.B st+
 .RB [ \-aiv ]
 .RB [ \-c
 .IR class ]
@@ -26,7 +26,7 @@ st \- simple terminal
 .IR command
 .RI [ arguments ...]]
 .PP
-.B st
+.B st+
 .RB [ \-aiv ]
 .RB [ \-c
 .IR class ]
@@ -48,8 +48,18 @@ st \- simple terminal
 .IR line
 .RI [ stty_args ...]
 .SH DESCRIPTION
-.B st
+.B st+
 is a simple terminal emulator.
+.SH APPIMAGE OPTIONS
+This section describes AppImage-specific commands and options. Run these commands from within the st+ terminal (may require root).
+
+.TP
+.B $APPDIR/st-config
+Menu utility to download fonts, color schemes and configure st+.
+
+.TP
+.B $APPDIR/st-config uninstall
+Uninstalls st+ and removes related files from the system.
 .SH OPTIONS
 .TP
 .B \-a
@@ -61,7 +71,7 @@ defines the window class (default $TERM).
 .BI \-f " font"
 defines the
 .I font
-to use when st is run.
+to use when st+ is run.
 .TP
 .BI \-g " geometry"
 defines the X11 geometry string.
@@ -78,17 +88,17 @@ defines the window instance name (default $TERM).
 .BI \-o " iofile"
 writes all the I/O to
 .I iofile.
-This feature is useful when recording st sessions. A value of "-" means
+This feature is useful when recording st+ sessions. A value of "-" means
 standard output.
 .TP
 .BI \-T " title"
-defines the window title (default 'st').
+defines the window title (default 'st+').
 .TP
 .BI \-t " title"
-defines the window title (default 'st').
+defines the window title (default 'st+').
 .TP
 .BI \-w " windowid"
-embeds st within the window identified by
+embeds st+ within the window identified by
 .I windowid
 .TP
 .BI \-l " line"
@@ -101,13 +111,13 @@ should be a (pseudo-)serial device (e.g. /dev/ttyS0 on Linux for serial port
 When this flag is given
 remaining arguments are used as flags for
 .BR stty(1).
-By default st initializes the serial line to 8 bits, no parity, 1 stop bit
+By default st+ initializes the serial line to 8 bits, no parity, 1 stop bit
 and a 38400 baud rate. The speed is set by appending it as last argument
-(e.g. 'st -l /dev/ttyS0 115200'). Arguments before the last one are
+(e.g. 'st+ -l /dev/ttyS0 115200'). Arguments before the last one are
 .BR stty(1)
-flags. If you want to set odd parity on 115200 baud use for example 'st -l
+flags. If you want to set odd parity on 115200 baud use for example 'st+ -l
 /dev/ttyS0 parenb parodd 115200'. Set the number of bits by using for
-example 'st -l /dev/ttyS0 cs7 115200'. See
+example 'st+ -l /dev/ttyS0 cs7 115200'. See
 .BR stty(1)
 for more arguments and cases.
 .TP
@@ -115,7 +125,7 @@ for more arguments and cases.
 prints version information to stderr, then exits.
 .TP
 .BI \-e " command " [ " arguments " "... ]"
-st executes
+st+ executes
 .I command
 instead of the shell.  If this is used it
 .B must be the last option
@@ -131,7 +141,7 @@ Break key is obtained in PC keyboards
 pressing at the same time control and pause.
 .TP
 .B Ctrl-Print Screen
-Toggle if st should print to the
+Toggle if st+ should print to the
 .I iofile.
 .TP
 .B Shift-Print Screen
@@ -160,9 +170,13 @@ Copy the selected text to the clipboard selection.
 .B Ctrl-Shift-v
 Paste from the clipboard selection.
 .SH CUSTOMIZATION
-.B st
-can be customized by creating a custom config.h and (re)compiling the source
-code. This keeps it fast, secure and simple.
+.B st+
+can be customized by editing the
+.B ~/.st.conf
+configuration file. Supported configuration values:
+.B font rows cols alpha colorname
+
+If this file does not exist or was deleted, it will be regenerated with safe defaults.
 .SH AUTHORS
 See the LICENSE file for the authors.
 .SH LICENSE
